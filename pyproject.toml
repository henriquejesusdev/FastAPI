[tool.poetry]
name = "task-api"
version = "0.1.0"
description = ""
authors = ["Henrique Jesus <henrique.jesus128@gmail.com>"]
readme = "README.md"
packages = [{include = "fastapi", from = "src"}]

[tool.poetry.dependencies]
python = "^3.13"
fastapi = "^0.104.0"
pydantic = "^2.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
httpx = "^0.24.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
